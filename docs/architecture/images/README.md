# Architecture Images

Screenshots referenced from the root [`README.md`](../../../README.md) "Architecture at a Glance"
section.

Expected file:

- `architecture-overview.png` — a rendered export of the high-level architecture diagram (either
  the root README's inline Mermaid diagram, or [`../containers.mmd`](../containers.mmd)), showing
  `auth-service` / `fraud-service` / `ledger-service`, their infrastructure dependencies, and the
  observability stack.

For the full, maintained set of C4 diagrams (system context, containers, components), see
[`docs/architecture/README.md`](../README.md) — those `.mmd` files are the source of truth; this
folder only holds a rendered snapshot for the root README's quick preview.

